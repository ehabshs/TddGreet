'use strict';

const program = require('commander');
const relativeToRoot = require('../lib/utils/relative-to-root');

program.option('--directory <path>', 'specs directory', relativeToRoot('petri-specs'))
  .parse(process.argv);

require('../lib/petri-specs').migrate(program);
require('../lib/petri-specs').create(program);
